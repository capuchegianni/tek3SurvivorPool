export type Employee = {
    id: number
    email: string
    name: string
    surname: string
    birth_date: string
    gender: string
    work: string
}

export type EmployeeDTO = {
    id: number
    email: string
    name: string
    surname: string
}